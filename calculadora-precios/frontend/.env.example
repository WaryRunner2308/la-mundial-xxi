VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# La clave de OpenRouter ya NO va aqui (con VITE_ queda expuesta en el bundle).
# Se configura como OPENROUTER_API_KEY (sin prefijo VITE_) en las variables
# de entorno del proyecto en Vercel, para que solo la vea frontend/api/scan-invoice.ts.
